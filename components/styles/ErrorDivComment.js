import styled from "styled-components";

export const ErrorDivComment = styled.div`
  display: flex;
  flex-wrap: wrap;
  background: rgba(255, 16, 16, 0.53);
  border-radius: 3px;
  margin-bottom: 1rem;

  p {
    padding: 1rem;
    font-size: 1.3rem;
    line-height: 1;
    font-weight: 600;
    align-self: flex-start;
  }
`;
